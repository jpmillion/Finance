<%= render partial: 'error_messages/full_messages', locals: { model: current_user } %>

<h1 class="jumbotron text-center">Edit Profile</h1>


<%= render partial: 'users/form', locals: { user: current_user }%>
