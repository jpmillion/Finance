<!DOCTYPE html>
<html>
  <head>
    <title>Finance</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


  </head>

  <body>
    <header>
      <%= button_to 'Sign In with GitHub', '/auth/github/callback' if !session[:user_id]%>
      <%= link_to('Log In', login_path) unless session[:user_id] %>
      <%= link_to('Register', new_user_path) unless session[:user_id] %>
    </header>
     <% flash.each do |name, msg| %>
       <%= content_tag :div, msg, class: name %>
     <% end %>

    <%= yield %>

    <br> <%= button_to('Log Out', session_path(current_user), method: :delete) if session[:user_id] %> 
  </body>
</html>
